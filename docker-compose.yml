services:
  app:
    build:
      context: .
    command: ["poetry", "run", "uvicorn", "src.wa_fin_ctrl.api:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      PYTHONPATH: /app
    restart: unless-stopped
