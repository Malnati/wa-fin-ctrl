---
description: Never load environment variables from files on disk (e.g. .env, .cursorignore, or any local config files). Always access environment variables directly from the runtime environment (process.env in Node.js, os.environ in Python, System.getenv in Java, etc.).
globs: ['*']
alwaysApply: true
---
